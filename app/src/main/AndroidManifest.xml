<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fcfm.lmad.poi.ChatPoi">
    
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatPoi">
        <activity android:name=".presentation.posts.view.NewPostActivity"></activity>
        <activity android:name=".NewTaskActivity" />
        <activity
            android:name=".presentation.teams.view.NewSubTeamActivity"
            android:label="@string/title_activity_new_sub_team"
            android:theme="@style/Theme.ChatPoi.NoActionBar" />
        <activity android:name=".presentation.chat.view.NewChatActivity" />
        <activity android:name=".presentation.register.view.RegisterActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".presentation.teams.view.TeamActivity" />
        <activity android:name=".TaskActivity" />
        <activity android:name=".presentation.chat.view.ChatRoomActivity" />
        <activity android:name=".presentation.main.view.MainActivity" />
        <activity android:name=".presentation.login.view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>